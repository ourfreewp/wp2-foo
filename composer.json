{
	"name": "wp2/foo",
	"description": "A template WP2 component for WordPress.",
	"type": "wordpress-plugin",
	"require": {
		"wp2/sdk": "^1.0"
	},
	"autoload": {
		"psr-4": {
			"WP2\\Foo\\": "src/"
		}
	},
	"scripts": {
		"test": "phpunit --configuration phpunit.xml.dist",
		"lint": "phpcs --standard=phpcs.xml.dist src/"
	}
}